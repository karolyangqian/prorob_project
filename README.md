# prorob_project

## How to Run
- Create container
```bash
sudo docker-compose up --build -d
```

- Enter container bash
```bash
sudo sh ./bash.sh
```

- Launch ROS2 process inside container
```bash
sudo sh ./launch.sh
```